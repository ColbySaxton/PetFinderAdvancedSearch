# 🐾 Petfinder Search App

A simple React + Express app that lets you search Petfinder API results by animal type, age, gender, and custom description keywords.

## 🚀 Setup

### 1️⃣ Backend
```bash
cd backend
npm install
npm start
```

### 2️⃣ Frontend
```bash
cd ../frontend
npx create-react-app .
npm start
```

Then replace `src/App.js` and `src/index.js` with the provided files.

The app will be available at **http://localhost:3000** and will connect to the backend running at **http://localhost:4000**.
